---

---
<html lang="es">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<slot>
      <div id="landscape-warning" class="landscape-warning">
    <div class="warning-content">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon-rotate">
            <rect x="5" y="2" width="14" height="20" rx="2" ry="2"></rect>
            <path d="M12 18h.01"></path>
            <path d="M4 12a8 8 0 0 1 14.93-3.7"></path>
            <polygon points="15 8 19 8 19 4"></polygon>
        </svg>
        
        <h2>¡Ups! Gira tu pantalla</h2>
        <p>Esta plataforma mágica funciona mucho mejor en formato horizontal.</p>
        
        <div class="hint-box">
            <p>💡 <strong>¿No gira?</strong> Recuerda activar la opción de <strong>rotación automática</strong> en tu dispositivo.</p>
        </div>
    </div>
      </div>

    </slot>
	</body>
</html>

<style is:global>
    /* =========================================
       2. TUS REGLAS DE ZOOM (Responsive)
       ========================================= */
    @media screen and (min-width: 1400px) {
        :root { --zoom-factor: calc(100vw / 1400px); }
        body {
            transform: scale(var(--zoom-factor));
            transform-origin: top center;
            overflow-x: hidden;
            justify-content: flex-start;
        }
    }
    @media screen and (max-width: 1400px) {
        :root { --zoom-factor: calc(100vw / 1400px); }
        body {
            transform: scale(var(--zoom-factor));
            transform-origin: top center;
            overflow-x: hidden;
            justify-content: flex-start;
        }
    }


    @media screen and (max-height: 650px) {
        :root { --zoom-factor-alto: calc(100vh / 650px); }
        body {
            transform: scale(var(--zoom-factor-alto));
            transform-origin: top center;
            overflow-x: hidden;
            justify-content: flex-start;
        }
    }
 
  /* --- FUENTE COINY --- */
  @font-face {
    font-family: 'Coiny';
    src: url('/fonts/Coiny-Regular.ttf') format('truetype');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  /* --- FUENTE FREDOKA --- */
  /* Registramos el peso Light */
  @font-face {
    font-family: 'Fredoka';
    src: url('/fonts/Fredoka-Light.ttf') format('truetype');
    font-weight: 300;
    font-style: normal;
    font-display: swap;
  }

  /* Registramos el peso Regular */
  @font-face {
    font-family: 'Fredoka';
    src: url('/fonts/Fredoka-Regular.ttf') format('truetype');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  /* Registramos el peso SemiBold */
  @font-face {
    font-family: 'Fredoka';
    src: url('/fonts/Fredoka-SemiBold.ttf') format('truetype');
    font-weight: 600;
    font-style: normal;
    font-display: swap;
  }


  /* 1. Reset básico para asegurar que las medidas son exactas y no hay márgenes molestos */
  * {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
  }
/* 2. El body: Maneja el FONDO GENERAL y el CENTRADO */
  body {
    /* --- CONFIGURACIÓN DE LA IMAGEN DE FONDO --- */
    background-image: url('/public/bg/fondoamarillo.svg'); 
    /* Asegura que la imagen cubra toda la pantalla sin repetirse y esté centrada */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    /* Evita que el fondo se mueva si hay scroll */
    background-attachment: fixed; 

    /* --- CONFIGURACIÓN DEL CENTRADO DEL CONTENEDOR --- */
    display: flex;
    justify-content: center; /* Centrado horizontal */
    align-items: flex-start;     /* Centrado vertical */
    min-height: 100vh;
    margin-top: 1%;       /* Asegura que el body ocupe todo el alto de la pantalla del navegador */
    
    /* --- COMPORTAMIENTO DE SCROLL --- */
    /* Si la ventana del navegador es más pequeña que 1400x650px, aparecerán barras de scroll */
    overflow: hidden;
  }

  

</style>